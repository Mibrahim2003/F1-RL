"""RL environment layer (Phase 3a).

The single-agent :class:`~f1rl.env.single_agent.RacingEnv`, the pure-NumPy
ObservationV1 builder (:mod:`f1rl.env.observations`, reused verbatim by the server),
RewardV1 (:mod:`f1rl.env.rewards`), conditions, and the SB3 vectorized-env factory.
"""
